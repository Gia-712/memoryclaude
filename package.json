{
  "name": "memoryclaude",
  "description": "Worker ponte ZAK (WuBook kapi) -> Notion PRENOTAZIONI.",
  "devDependencies": {
    "typescript": "5.8.2",
    "wrangler": "4.7.0"
  },
  "scripts": {
    "check": "tsc && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "wrangler types"
  }
}
